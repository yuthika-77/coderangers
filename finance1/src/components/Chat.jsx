import React from 'react';
import ChatInterface from './ChatInterface';

const Chat = () => {
    return (
        <div style={{ height: '100%', width: '100%' }}>
            <ChatInterface />
        </div>
    );
};

export default Chat; 